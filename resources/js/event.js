/*import Vue from 'vue';

window.Event = new Vue();

export default new Vue();*/
import { createApp } from 'vue';

const app = createApp({});
export default new Vue();
//app.config.globalProperties.$Event = new Vue();